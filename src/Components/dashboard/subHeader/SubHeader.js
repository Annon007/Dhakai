import React from "react";
import styles from "./subHeader.module.css";

const SubHeader = () => {
    return <div className={styles.subHeaderContainer}>
        <div>
            <button className={`${styles.menuBtn} ${styles.btnActive}`}>All&nbsp;for&nbsp;you</button>
            <button className={`${styles.menuBtn}`}>Recommended</button>
            <button className={`${styles.menuBtn}`}>Recommended</button>
            <button className={`${styles.menuBtn}`}>Recommended</button>
            <button className={`${styles.menuBtn}`}>Recommended</button>
            <button className={`${styles.menuBtn}`}>Recommended</button>
            <button className={`${styles.menuBtn}`}>Recommended</button>
            <button className={`${styles.menuBtn}`}>Recommended</button>
            <button className={`${styles.menuBtn}`}>Recommended</button>
            <button className={`${styles.menuBtn}`}>Recommended</button>
            <button className={`${styles.menuBtn}`}>Recommended</button>
            <button className={`${styles.menuBtn}`}>Recommended</button>
            <button className={`${styles.menuBtn}`}>Recommended</button>
            <button className={`${styles.menuBtn}`}>Recommended</button>
            <button className={`${styles.menuBtn}`}>Recommended</button>
            <button className={`${styles.menuBtn}`}>Recommended</button>
            <button className={`${styles.menuBtn}`}>Recommended</button>
            <button className={`${styles.menuBtn}`}>Recommended</button>
            <button className={`${styles.menuBtn}`}>Recommended</button>
            <button className={`${styles.menuBtn}`}>Recommended</button>
            <button className={`${styles.menuBtn}`}>Recommended</button>
            <button className={`${styles.menuBtn}`}>Recommended</button>
            <button className={`${styles.menuBtn}`}>Recommended</button>
            <button className={`${styles.menuBtn}`}>Recommended</button>
        </div>
    </div>
};

export default SubHeader;